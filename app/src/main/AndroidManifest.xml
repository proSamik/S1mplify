<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.s1mplify">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.S1mplify">
        <activity android:name=".QuotesView"></activity>
        <activity
            android:name=".main"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.S1mplify.NoActionBar"> <!--  -->
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Donate" />
        <activity android:name=".Forum" />
        <activity android:name=".Test" />
        <activity android:name=".Information" />
        <activity android:name=".Learn" />
        <activity android:name=".Profile" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>